/**
 * dsh-precise-cache, node half.
 *
 * Deliberately empty. The readout is pure browser presentation: the client
 * half registers a second `conversation.composer.dock` entry beside the
 * shipped stats line and reads the host-folded `tokenUsage` projection.
 * Nothing here registers a host Service, tool, or prompt contribution.
 */

/** Host plugin body — no host capability lives in this package. */
export function apply() {}
